# 认证服务单元测试用例文档

> **模块名称**: 认证服务 (AuthService)  
> **测试类**: `AuthServiceTest.java`  
> **测试框架**: JUnit 5 + Mockito  
> **创建日期**: 2026-01-15  
> **版本**: v1.0

---

## 📋 文档概述

本文档描述了GCT Reporter系统认证服务（AuthService）的单元测试用例，涵盖用户登录、密码验证、Token生成等核心功能的测试场景。

### 测试目标

- 验证用户登录功能的正确性
- 确保密码验证机制的安全性
- 验证Token生成规则的准确性
- 测试各种异常场景的处理

---

## 🔧 测试环境

### 技术栈

| 组件 | 版本 | 说明 |
|------|------|------|
| JUnit | 5.x | 单元测试框架 |
| Mockito | 5.x | Mock框架 |
| Spring Boot | 3.1.5 | 应用框架 |
| BCrypt | - | 密码加密算法 |

### Mock对象

- `UserRepository`: 用户数据访问层Mock
- `BCryptPasswordEncoder`: 密码加密器Mock

---

## 📊 测试数据

### 标准测试用户

```java
User testUser = {
    id: 1L,
    username: "admin",
    password: "$2a$10$N.zmdr9k7uOCQb376NoUnuTJ8iAt6Z5EHsM8lE9lBOsl7iKTVKIUi",
    // 原始密码: "admin123"
    role: ADMIN,
    enabled: true,
    createdAt: LocalDateTime.now(),
    updatedAt: LocalDateTime.now()
}
```

### 标准登录请求

```java
LoginRequest loginRequest = {
    username: "admin",
    password: "admin123"
}
```

---

## 📝 测试用例清单

| 用例ID | 用例名称 | 优先级 | 状态 |
|--------|---------|--------|------|
| TC-AUTH-001 | 登录成功-正确的用户名和密码 | P0 | ✅ 通过 |
| TC-AUTH-002 | 登录失败-用户不存在 | P0 | ✅ 通过 |
| TC-AUTH-003 | 登录失败-密码错误 | P0 | ✅ 通过 |
| TC-AUTH-004 | 登录失败-用户已禁用 | P1 | ✅ 通过 |
| TC-AUTH-005 | Token生成格式验证 | P1 | ✅ 通过 |

---

## 🧪 详细测试用例

### TC-AUTH-001: 登录成功 - 正确的用户名和密码

**测试方法**: `login_Success()`  
**优先级**: P0 (核心功能)  
**测试类型**: 正向测试

#### 📌 测试目标
验证使用正确的用户名和密码能够成功登录系统。

#### 📋 前置条件
1. 数据库中存在启用状态的测试用户
2. 用户名：`admin`
3. 密码：`admin123`（加密后存储）
4. 用户角色：`ADMIN`

#### 🔧 Mock配置

```java
// 1. Mock用户查询返回测试用户
when(userRepository.findByUsernameAndEnabled("admin", true))
    .thenReturn(Optional.of(testUser));

// 2. Mock密码验证通过
when(passwordEncoder.matches("admin123", testUser.getPassword()))
    .thenReturn(true);
```

#### 📝 测试步骤

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 创建登录请求对象（username="admin", password="admin123"） | 对象创建成功 |
| 2 | 调用 `authService.login(loginRequest)` | 方法执行无异常 |
| 3 | 验证返回的 LoginResponse 对象 | 对象不为null |

#### ✅ 预期结果

```java
LoginResponse response = {
    token: "TOKEN_{userId}_{timestamp}", // 格式: TOKEN_1_1737878400000
    username: "admin",
    role: "ADMIN",
    userId: 1
}
```

#### 🔍 验证点

- [x] `response` 不为 `null`
- [x] `response.getUsername()` 等于 `"admin"`
- [x] `response.getRole()` 等于 `"ADMIN"`
- [x] `response.getUserId()` 等于 `1L`
- [x] `response.getToken()` 不为 `null`
- [x] `response.getToken()` 以 `"TOKEN_"` 开头

#### 📊 业务价值
确保合法用户能够正常登录系统，是系统最基础的功能验证。

---

### TC-AUTH-002: 登录失败 - 用户不存在

**测试方法**: `login_UserNotFound()`  
**优先级**: P0 (安全功能)  
**测试类型**: 异常测试

#### 📌 测试目标
验证输入不存在的用户名时系统能够正确拒绝登录并返回友好错误信息。

#### 📋 前置条件
1. 输入的用户名在数据库中不存在
2. 或用户存在但已被禁用（enabled=false）

#### 🔧 Mock配置

```java
// Mock用户查询返回空（用户不存在）
when(userRepository.findByUsernameAndEnabled(anyString(), anyBoolean()))
    .thenReturn(Optional.empty());
```

#### 📝 测试步骤

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 创建登录请求（username="nonexistent", password="any"） | 对象创建成功 |
| 2 | 调用 `authService.login(loginRequest)` | 抛出 `BusinessException` |
| 3 | 验证异常信息 | 错误信息为"用户名或密码错误" |

#### ✅ 预期结果

- 抛出 `BusinessException` 异常
- 异常消息：`"用户名或密码错误"`

#### 🔍 验证点

- [x] 方法抛出 `BusinessException` 异常
- [x] 异常消息等于 `"用户名或密码错误"`
- [x] 未返回任何用户信息（安全性考虑）

#### 🔒 安全考量
错误信息统一为"用户名或密码错误"，不区分用户是否存在，避免暴露系统用户信息。

---

### TC-AUTH-003: 登录失败 - 密码错误

**测试方法**: `login_WrongPassword()`  
**优先级**: P0 (安全功能)  
**测试类型**: 异常测试

#### 📌 测试目标
验证输入错误密码时系统能够正确拒绝登录。

#### 📋 前置条件
1. 用户存在且启用
2. 输入的密码与存储的密码不匹配

#### 🔧 Mock配置

```java
// 1. Mock用户查询返回测试用户
when(userRepository.findByUsernameAndEnabled("admin", true))
    .thenReturn(Optional.of(testUser));

// 2. Mock密码验证失败
when(passwordEncoder.matches("wrongpassword", testUser.getPassword()))
    .thenReturn(false);
```

#### 📝 测试步骤

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 创建登录请求（username="admin", password="wrongpassword"） | 对象创建成功 |
| 2 | 调用 `authService.login(wrongRequest)` | 抛出 `BusinessException` |
| 3 | 验证异常信息 | 错误信息为"用户名或密码错误" |

#### ✅ 预期结果

- 抛出 `BusinessException` 异常
- 异常消息：`"用户名或密码错误"`

#### 🔍 验证点

- [x] 方法抛出 `BusinessException` 异常
- [x] 异常消息等于 `"用户名或密码错误"`
- [x] 调用了 `passwordEncoder.matches()` 方法验证密码
- [x] 未返回Token或用户信息

#### 🔒 安全考量
使用BCrypt算法验证密码，确保密码安全性。

---

### TC-AUTH-004: 登录失败 - 用户已禁用

**测试方法**: `login_UserDisabled()`  
**优先级**: P1 (权限控制)  
**测试类型**: 异常测试

#### 📌 测试目标
验证已禁用的用户无法登录系统。

#### 📋 前置条件
1. 用户存在但 `enabled` 字段为 `false`
2. 查询条件要求 `enabled=true`

#### 🔧 Mock配置

```java
// Mock查询启用用户时返回空（因为用户已禁用）
when(userRepository.findByUsernameAndEnabled("admin", true))
    .thenReturn(Optional.empty());
```

#### 📝 测试步骤

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 创建正确的登录请求（用户已禁用） | 对象创建成功 |
| 2 | 调用 `authService.login(loginRequest)` | 抛出 `BusinessException` |
| 3 | 验证异常信息 | 错误信息为"用户名或密码错误" |

#### ✅ 预期结果

- 抛出 `BusinessException` 异常
- 异常消息：`"用户名或密码错误"`

#### 🔍 验证点

- [x] 方法抛出 `BusinessException` 异常
- [x] 异常消息等于 `"用户名或密码错误"`
- [x] 查询使用了 `enabled=true` 条件

#### 📊 业务价值
支持管理员禁用用户功能，增强系统安全性和管理灵活性。

---

### TC-AUTH-005: Token生成格式验证

**测试方法**: `generateToken_Format()`  
**优先级**: P1 (功能验证)  
**测试类型**: 正向测试

#### 📌 测试目标
验证生成的Token符合预定义的格式规范。

#### 📋 前置条件
1. 用户成功登录
2. Token生成逻辑正常工作

#### 🔧 Mock配置

```java
// 1. Mock用户查询返回测试用户
when(userRepository.findByUsernameAndEnabled("admin", true))
    .thenReturn(Optional.of(testUser));

// 2. Mock密码验证通过
when(passwordEncoder.matches("admin123", testUser.getPassword()))
    .thenReturn(true);
```

#### 📝 测试步骤

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 执行成功登录 | 获得 LoginResponse 对象 |
| 2 | 提取 `response.getToken()` | Token字符串不为null |
| 3 | 使用正则表达式验证Token格式 | 匹配 `TOKEN_\d+_\d+` 模式 |

#### ✅ 预期结果

Token格式规范：
```
格式: TOKEN_{userId}_{timestamp}
示例: TOKEN_1_1737878400000
正则: ^TOKEN_\d+_\d+$
```

#### 🔍 验证点

- [x] Token不为 `null`
- [x] Token以 `"TOKEN_"` 开头
- [x] Token包含用户ID（数字）
- [x] Token包含时间戳（数字）
- [x] Token格式匹配正则表达式 `TOKEN_\d+_\d+`

#### 🔧 技术说明
当前使用简单Token格式（`userId_timestamp`），生产环境应升级为JWT Token以支持：
- Token过期时间控制
- Token签名验证
- Token payload信息加密
- Token刷新机制

---

## 📈 测试覆盖率

### 功能覆盖率

| 功能模块 | 测试用例数 | 覆盖场景 | 覆盖率 |
|---------|-----------|---------|--------|
| 用户登录 | 4 | 成功/失败场景 | 100% |
| 密码验证 | 2 | 正确/错误密码 | 100% |
| Token生成 | 1 | 格式验证 | 100% |
| 异常处理 | 3 | 各种错误场景 | 100% |

### 代码覆盖率目标

- **行覆盖率**: > 80%
- **分支覆盖率**: > 75%
- **方法覆盖率**: 100%

---

## 🔄 测试执行

### Maven命令

```bash
# 执行所有测试
mvn test

# 执行指定测试类
mvn test -Dtest=AuthServiceTest

# 执行单个测试方法
mvn test -Dtest=AuthServiceTest#login_Success

# 生成测试报告
mvn surefire-report:report
```

### 预期输出

```
[INFO] Tests run: 5, Failures: 0, Errors: 0, Skipped: 0
[INFO] ------------------------------------------------------------------------
[INFO] BUILD SUCCESS
[INFO] ------------------------------------------------------------------------
```

---

## 🐛 已知问题与改进建议

### 当前限制

1. **Token安全性**
   - 现状：使用简单的 `userId_timestamp` 格式
   - 风险：容易被预测和伪造
   - 建议：升级为JWT Token

2. **密码策略**
   - 现状：未限制密码强度
   - 建议：增加密码复杂度验证（长度、特殊字符等）

3. **登录失败限制**
   - 现状：未实现登录失败次数限制
   - 建议：增加账号锁定机制（如5次失败锁定30分钟）

### 改进计划

| 优先级 | 改进项 | 目标版本 | 负责人 |
|-------|-------|---------|--------|
| P0 | 实现JWT Token | v1.1 | TBD |
| P1 | 增加密码强度验证 | v1.2 | TBD |
| P1 | 实现登录失败限制 | v1.2 | TBD |
| P2 | 增加双因素认证 | v2.0 | TBD |

---

## 📚 参考资料

### 相关文档

- [AuthService源码](../src/main/java/com/gct/reportgenerator/service/AuthService.java)
- [用户故事US001](../../docs/US001-用户登录功能.md)
- [API文档](http://localhost:8080/swagger-ui.html)

### 技术标准

- [OWASP密码存储指南](https://cheatsheetseries.owasp.org/cheatsheets/Password_Storage_Cheat_Sheet.html)
- [JWT最佳实践](https://datatracker.ietf.org/doc/html/rfc8725)
- [Spring Security文档](https://docs.spring.io/spring-security/reference/index.html)

---

## 📊 测试执行记录

| 执行日期 | 执行人 | 测试环境 | 通过数 | 失败数 | 备注 |
|---------|-------|---------|--------|--------|------|
| 2026-01-15 | 系统 | 开发环境 | 5 | 0 | 初始版本所有测试通过 |

---

## 📝 变更历史

| 版本 | 日期 | 变更内容 | 作者 |
|------|------|---------|------|
| v1.0 | 2026-01-15 | 初始版本，创建5个核心测试用例 | AI |

---

**文档维护**: GCT Reporter开发团队  
**最后更新**: 2026-01-15  
**下次审查**: 2026-02-15
