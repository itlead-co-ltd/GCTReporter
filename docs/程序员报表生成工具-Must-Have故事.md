# 程序员报表生成工具 - Must Have 用户故事

> **文档类型**: MVP核心用户故事清单（Must Have Stories）
> **生成日期**: 2025-01-25
> **来源文档**: 程序员报表生成工具-用户故事列表.md
> **优先级**: Must Have (30个故事)
> **预估工作量**: 约38人日（2人团队，约19个工作日）

---

## 📋 Must Have 故事总览

| 故事ID | 故事标题 | 用户角色 | 功能分组 | 粒度 | 工作量估算 |
|--------|---------|---------|---------|------|------------|
| US000 | **开发环境搭建与项目初始化** | 系统开发者 | 基础设施层 | M | **1天** |
| US001 | 用户登录功能 | 系统用户 | 基础设施层 | S | 0.5天 |
| US002 | 用户登出功能 | 系统用户 | 基础设施层 | S | 0.5天 |
| US003 | Session管理与认证拦截器 | 系统开发者 | 基础设施层 | M | 2天 |
| US004 | 数据库表结构初始化 | 系统开发者 | 基础设施层 | M | 1.5天 |
| US005 | 创建用户账号 | 管理员 | 管理端 | S | 0.5天 |
| US006 | 编辑用户信息 | 管理员 | 管理端 | S | 0.5天 |
| US007 | 禁用/启用用户 | 管理员 | 管理端 | S | 0.5天 |
| US008 | 为用户分配角色 | 管理员 | 管理端 | S | 0.5天 |
| US010 | **SQL编辑器实现（CodeMirror）** | 设计者 | 设计端-核心 | M | **2天** |
| US011 | 报表基本信息配置 | 设计者 | 设计端-核心 | S | 0.5天 |
| US012 | 参数配置功能 | 设计者 | 设计端-核心 | M | 1.5天 |
| US013 | **列配置功能** | 设计者 | 设计端-核心 | M | **2天** |
| US014 | **报表创建功能** | 设计者 | 设计端-核心 | S | **1天** |
| US015 | 报表编辑功能 | 设计者 | 设计端-核心 | S | 0.5天 |
| US016 | 报表删除功能 | 设计者 | 设计端-核心 | S | 0.5天 |
| US017 | **报表预览（测试执行）** | 设计者 | 设计端-预览与授权 | M | **2.5天** |
| US018 | 为报表分配角色权限 | 设计者 | 设计端-预览与授权 | M | 2天 |
| US021 | 查看授权报表列表 | 普通用户 | 用户端-查询 | M | 1.5天 |
| US022 | 查看报表详情 | 普通用户 | 用户端-查询 | S | 0.5天 |
| US023 | **参数输入表单** | 普通用户 | 用户端-查询 | M | **2天** |
| US024 | 执行报表查询 | 普通用户 | 用户端-查询 | M | 2天 |
| US025 | **数据表格展示** | 普通用户 | 用户端-展示与导出 | M | **2天** |
| US026 | 分页功能 | 普通用户 | 用户端-展示与导出 | S | 0.5天 |
| US028 | Excel导出功能 | 普通用户 | 用户端-展示与导出 | M | 2天 |
| US029 | 角色定义与权限矩阵 | 系统开发者 | 权限控制层 | M | 1.5天 |
| US030 | 权限拦截器实现 | 系统开发者 | 权限控制层 | M | 1.5天 |
| US032 | 执行日志记录 | 系统开发者 | 权限控制层 | S | 1天 |
| US033 | SQL安全校验 | 系统开发者 | 技术增强 | M | 1.5天 |
| US034 | **动态列渲染优化** | 系统开发者 | 技术增强 | M | **2天** |

**关键变更说明**（已应用INVEST评估优化建议）:
- ✅ **US000: 新增开发环境搭建故事**，工作量1天
- ✅ US010: 明确使用CodeMirror 5.65.x，工作量调整为2天
- ✅ US013: 明确依赖US010/US017，工作量调整为2天
- ✅ US014: 增加详细验收标准，工作量调整为1天
- ✅ US017: 明确技术依赖，工作量调整为2.5天
- ✅ US023: 明确支持的参数类型，工作量调整为2天
- ✅ US025: 明确依赖US034，工作量调整为2天
- ✅ **US034: 提升为Must Have**，明确业务价值，工作量调整为2天

---

## 🏗️ 功能分组1：基础设施层

### US000: 开发环境搭建与项目初始化

**用户故事**
> 作为 系统开发者，
> 我希望 搭建完整的开发环境并初始化项目结构，
> 以便 团队能够立即开始功能开发。

**功能要点**

#### 1. 技术栈选型与版本确认
- **前端技术栈**
  - Vue 3.3.x（组合式API）
  - Vite 4.x（构建工具）
  - Element Plus 2.3.x（UI组件库）
  - vue-codemirror 4.0.6（代码编辑器）
  - CodeMirror 5.65.x（SQL语法高亮）
  - Axios 1.x（HTTP客户端）
  - Vue Router 4.x（路由管理）
  - Pinia 2.x（状态管理）
  
- **后端技术栈**
  - Java 17 LTS
  - SpringBoot 3.1.x
  - Spring Web（RESTful API）
  - Spring Data JPA（ORM）
  - MySQL 8.0+（数据库）
  - Lombok（代码简化）
  - Apache POI 5.x（Excel导出）
  - Flyway 9.x（数据库版本管理）
  
- **开发工具**
  - IntelliJ IDEA（推荐）或 VS Code
  - Maven 3.8+（依赖管理）
  - Node.js 18.x LTS + npm 9.x（前端依赖管理）
  - Git 2.x（版本控制）
  - Postman（API测试）

#### 2. 前端项目初始化
- 使用Vite创建Vue 3项目：`npm create vite@latest report-generator-web -- --template vue`
- 安装核心依赖：Element Plus、Axios、Vue Router、Pinia
- 安装CodeMirror：`npm install vue-codemirror@4.0.6 codemirror@5.65.x`
- 配置Vite代理（开发环境API代理到后端8080端口）
- 创建标准目录结构：
  ```
  report-generator-web/
  ├── src/
  │   ├── api/           # API接口封装
  │   ├── assets/        # 静态资源
  │   ├── components/    # 可复用组件
  │   ├── router/        # 路由配置
  │   ├── stores/        # Pinia状态管理
  │   ├── utils/         # 工具函数
  │   ├── views/         # 页面组件
  │   ├── App.vue        # 根组件
  │   └── main.js        # 入口文件
  ├── public/            # 静态资源（不经过打包）
  ├── index.html         # HTML模板
  ├── vite.config.js     # Vite配置
  ├── package.json       # 依赖配置
  └── .gitignore         # Git忽略配置
  ```

#### 3. 后端项目初始化
- 使用Spring Initializr创建SpringBoot项目（https://start.spring.io/）
  - Group: com.itlead
  - Artifact: report-generator
  - Dependencies: Spring Web, Spring Data JPA, MySQL Driver, Lombok, Flyway Migration
- 配置application.yml（开发环境配置）：
  ```yaml
  server:
    port: 8080
  spring:
    datasource:
      url: jdbc:mysql://localhost:3306/report_generator?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai
      username: root
      password: root
      driver-class-name: com.mysql.cj.jdbc.Driver
    jpa:
      hibernate:
        ddl-auto: validate
      show-sql: true
    flyway:
      enabled: true
      baseline-on-migrate: true
      locations: classpath:db/migration
  ```
- 创建标准目录结构：
  ```
  report-generator/
  ├── src/main/java/com/itlead/reportgenerator/
  │   ├── config/        # 配置类（CORS、拦截器）
  │   ├── controller/    # REST控制器
  │   ├── service/       # 业务逻辑层
  │   ├── repository/    # 数据访问层
  │   ├── entity/        # 实体类
  │   ├── dto/           # 数据传输对象
  │   ├── util/          # 工具类
  │   └── ReportGeneratorApplication.java
  ├── src/main/resources/
  │   ├── db/migration/  # Flyway数据库脚本
  │   ├── application.yml
  │   └── application-dev.yml
  ├── src/test/java/     # 测试代码
  ├── pom.xml            # Maven配置
  └── .gitignore         # Git忽略配置
  ```

#### 4. 数据库初始化脚本
- 创建数据库：`CREATE DATABASE report_generator CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;`
- 创建Flyway迁移脚本：`src/main/resources/db/migration/V1__init_schema.sql`
  ```sql
  -- 用户表
  CREATE TABLE users (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    username VARCHAR(50) NOT NULL UNIQUE COMMENT '用户名',
    password VARCHAR(100) NOT NULL COMMENT '密码（BCrypt加密）',
    role VARCHAR(20) NOT NULL COMMENT '角色：ADMIN/DESIGNER/VIEWER',
    enabled BOOLEAN DEFAULT TRUE COMMENT '是否启用',
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    INDEX idx_username (username),
    INDEX idx_role (role)
  ) COMMENT='用户表';
  
  -- 报表表
  CREATE TABLE reports (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    name VARCHAR(100) NOT NULL UNIQUE COMMENT '报表名称',
    description TEXT COMMENT '报表描述',
    sql_content TEXT NOT NULL COMMENT 'SQL查询语句',
    creator_id BIGINT NOT NULL COMMENT '创建人ID',
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    deleted BOOLEAN DEFAULT FALSE COMMENT '软删除标记',
    INDEX idx_name (name),
    INDEX idx_creator (creator_id),
    FOREIGN KEY (creator_id) REFERENCES users(id)
  ) COMMENT='报表表';
  
  -- 参数表
  CREATE TABLE report_params (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    report_id BIGINT NOT NULL,
    param_name VARCHAR(50) NOT NULL COMMENT '参数名',
    param_type VARCHAR(20) NOT NULL COMMENT '参数类型：STRING/NUMBER/DATE/DATETIME',
    default_value VARCHAR(200) COMMENT '默认值',
    required BOOLEAN DEFAULT FALSE COMMENT '是否必填',
    description VARCHAR(200) COMMENT '参数说明',
    sort_order INT DEFAULT 0 COMMENT '排序序号',
    INDEX idx_report (report_id),
    FOREIGN KEY (report_id) REFERENCES reports(id) ON DELETE CASCADE
  ) COMMENT='报表参数表';
  
  -- 列配置表
  CREATE TABLE report_columns (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    report_id BIGINT NOT NULL,
    field_name VARCHAR(50) NOT NULL COMMENT '字段名',
    display_name VARCHAR(50) NOT NULL COMMENT '显示名称',
    column_width INT DEFAULT 100 COMMENT '列宽（像素）',
    format_type VARCHAR(20) DEFAULT 'DEFAULT' COMMENT '格式化类型：DEFAULT/DATE/NUMBER/CURRENCY',
    visible BOOLEAN DEFAULT TRUE COMMENT '是否显示',
    sort_order INT DEFAULT 0 COMMENT '排序序号',
    INDEX idx_report (report_id),
    FOREIGN KEY (report_id) REFERENCES reports(id) ON DELETE CASCADE
  ) COMMENT='列配置表';
  
  -- 权限表
  CREATE TABLE report_permissions (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    report_id BIGINT NOT NULL,
    role VARCHAR(20) NOT NULL COMMENT '角色：ADMIN/DESIGNER/VIEWER',
    INDEX idx_report (report_id),
    INDEX idx_role (role),
    UNIQUE KEY uk_report_role (report_id, role),
    FOREIGN KEY (report_id) REFERENCES reports(id) ON DELETE CASCADE
  ) COMMENT='报表权限表';
  
  -- 执行日志表
  CREATE TABLE execution_logs (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    user_id BIGINT NOT NULL,
    report_id BIGINT NOT NULL,
    params_json TEXT COMMENT '参数JSON',
    execute_time DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '执行时间',
    result_rows INT COMMENT '结果行数',
    success BOOLEAN DEFAULT TRUE COMMENT '是否成功',
    error_message TEXT COMMENT '错误信息',
    INDEX idx_user (user_id),
    INDEX idx_report (report_id),
    INDEX idx_time (execute_time),
    FOREIGN KEY (user_id) REFERENCES users(id),
    FOREIGN KEY (report_id) REFERENCES reports(id)
  ) COMMENT='执行日志表';
  ```

- 创建初始化数据脚本：`src/main/resources/db/migration/V2__init_data.sql`
  ```sql
  -- 插入默认管理员账号（密码：admin123，BCrypt加密）
  INSERT INTO users (username, password, role, enabled) VALUES 
  ('admin', '$2a$10$N.zmdr9k7uOCQb376NoUnuTJ8iAt6Z5EHsM8lE9lBOsl7iKTVKIUi', 'ADMIN', TRUE);
  
  -- 插入测试设计者账号（密码：designer123）
  INSERT INTO users (username, password, role, enabled) VALUES 
  ('designer', '$2a$10$5Z9XKvSymhzkrOXf8YpMiOFq3RLncBd2WlR7yNIJeV9v1H.zXB3lq', 'DESIGNER', TRUE);
  
  -- 插入测试查看者账号（密码：viewer123）
  INSERT INTO users (username, password, role, enabled) VALUES 
  ('viewer', '$2a$10$U3ZJm0yQYJZXHdK0NjqnGekj1Kz8.JZ3OQJzZ5P5YH8Q8VHqGz7Ky', 'VIEWER', TRUE);
  ```

#### 5. Git版本控制配置
- 初始化Git仓库：`git init`
- 创建.gitignore文件（排除node_modules、target、.idea、.DS_Store等）
- 创建README.md（项目说明文档）
- 提交初始代码：
  ```bash
  git add .
  git commit -m "feat: 初始化项目结构"
  ```

#### 6. 开发服务器启动验证
- 启动MySQL数据库服务
- 启动后端服务：`mvn spring-boot:run`（访问 http://localhost:8080）
- 启动前端服务：`npm run dev`（访问 http://localhost:5173）
- 验证Flyway脚本执行成功（检查数据库表是否创建）
- 验证前端可以访问后端API（配置CORS）

**业务规则**
- 所有开发者必须使用统一的技术栈版本
- 数据库脚本使用Flyway管理，禁止手动修改数据库
- 代码提交前必须通过本地编译和基础测试
- 敏感配置（数据库密码）不能提交到Git仓库

**技术要点**
- Flyway版本号规则：V{版本号}__{描述}.sql（如V1__init_schema.sql）
- SpringBoot配置使用YAML格式（易读性更好）
- 前端使用ESM模块规范（import/export）
- 后端使用分层架构（Controller → Service → Repository）

**验收标准**
- [ ] 前端项目可以正常启动（npm run dev无错误）
- [ ] 后端项目可以正常启动（mvn spring-boot:run无错误）
- [ ] 数据库表结构创建成功（6张表）
- [ ] 初始化数据插入成功（3个测试账号）
- [ ] 前端可以通过代理访问后端API
- [ ] Git仓库初始化完成（.gitignore配置正确）
- [ ] README.md文档完整（包含启动步骤）
- [ ] 所有依赖下载成功（npm install、mvn dependency:resolve）

**界面要求**
- 前端启动后显示默认欢迎页面
- 后端启动后访问根路径返回JSON：`{"status": "ok", "message": "Report Generator API is running"}`

**粒度评估**: M (Medium) - 1天工作量（前端初始化0.5天 + 后端初始化0.5天）

**依赖关系**: 无（这是第一个要完成的Story）

**风险提示**
- ⚠️ Flyway首次执行可能因权限问题失败，需要检查MySQL用户权限
- ⚠️ CodeMirror版本必须是5.65.x，6.x版本API不兼容
- ⚠️ Node.js版本必须≥18.x，否则Vite可能无法启动
- ⚠️ Java必须是17 LTS，SpringBoot 3.x不支持Java 11

---

### US001: 用户登录功能

**用户故事**
> 作为 系统用户，
> 我希望 能够通过用户名和密码登录系统，
> 以便 访问系统功能。

**功能要点**
- 提供登录页面，包含用户名、密码输入框
- 点击"登录"按钮后验证用户名和密码
- 验证成功后创建Session并跳转到首页
- 验证失败时显示错误提示

**业务规则**
- 用户名和密码不能为空
- 密码在前端加密传输（可选，使用HTTPS更安全）
- 登录失败3次后锁定账号5分钟（可选）
- 禁用状态的用户无法登录

**界面要求**
- 简洁的登录页面，包含Logo和标题
- 友好的错误提示（如"用户名或密码错误"）

**粒度评估**: S (Small) - 0.5天工作量，标准登录功能

---

### US002: 用户登出功能

**用户故事**
> 作为 系统用户，
> 我希望 能够安全地退出系统，
> 以便 保护我的账号信息。

**功能要点**
- 提供"退出登录"按钮（位于页面右上角）
- 点击后清除Session
- 跳转到登录页面

**业务规则**
- 退出后Session立即失效
- 退出后需要重新登录才能访问系统

**界面要求**
- "退出登录"按钮明显且易于点击
- 退出后页面无闪烁，平滑跳转到登录页

**粒度评估**: S (Small) - 0.5天工作量，标准登出功能

---

### US003: Session管理与认证拦截器

**用户故事**
> 作为 系统开发者，
> 我希望 实现Session管理和认证拦截器，
> 以便 确保只有登录用户才能访问系统功能。

**功能要点**
- SpringBoot配置Session管理（使用HttpSession或Spring Session）
- 实现拦截器（Interceptor）拦截所有请求
- 校验Session有效性：未登录用户跳转到登录页
- Session超时时间：30分钟无操作自动过期

**业务规则**
- 登录页和静态资源（CSS、JS、图片）不需要拦截
- 其他所有页面和API都需要登录后才能访问
- Session过期后自动跳转到登录页

**技术要点**
- SpringBoot拦截器（HandlerInterceptor）
- Session管理（HttpSession）
- 白名单路径配置（/login, /static/**）

**粒度评估**: M (Medium) - 2天工作量，涉及Spring Security或自定义拦截器

---

### US004: 数据库表结构初始化

**用户故事**
> 作为 系统开发者，
> 我希望 初始化数据库表结构（用户、报表、参数、列配置、权限、日志），
> 以便 系统能够正常运行。

**功能要点**
- 创建数据库表结构脚本（SQL文件）
- 表设计：users（用户表）、reports（报表表）、report_params（参数表）、report_columns（列配置表）、report_permissions（权限表）、execution_logs（执行日志表）
- 初始化数据：创建默认管理员账号（admin/admin123）、创建三个角色（ADMIN、DESIGNER、VIEWER）

**业务规则**
- 使用Flyway或Liquibase管理数据库版本（可选）
- 表结构需要支持软删除（deleted字段）
- 所有表都需要created_at和updated_at字段

**技术要点**
- MySQL数据库（或其他关系型数据库）
- 数据库迁移工具（Flyway/Liquibase）
- 初始化脚本（schema.sql, data.sql）

**粒度评估**: M (Medium) - 1.5天工作量，涉及数据库设计和初始化

---

## 🔐 功能分组2：管理端（ADMIN）

### US005: 创建用户账号

**用户故事**
> 作为 管理员，
> 我希望 能够创建新的用户账号，
> 以便 让新用户使用系统。

**功能要点**
- 填写用户名、密码、角色（ADMIN/DESIGNER/VIEWER）
- 点击"创建"按钮后保存用户信息
- 密码在后端加密存储（使用BCrypt）
- 创建成功后跳转到用户列表

**业务规则**
- 用户名唯一（不能重复）
- 密码至少6位
- 新用户默认状态为"启用"

**界面要求**
- 用户创建表单，字段清晰
- 密码输入框支持显示/隐藏密码

**粒度评估**: S (Small) - 0.5天工作量，标准CRUD功能

---

### US006: 编辑用户信息

**用户故事**
> 作为 管理员，
> 我希望 能够编辑用户的基本信息（角色、密码），
> 以便 修正错误或更新用户权限。

**功能要点**
- 从用户列表中选择用户并编辑
- 可以修改角色和密码
- 密码留空表示不修改
- 保存后更新用户信息

**业务规则**
- 不能修改用户名（用户名唯一且不可变）
- 修改密码时需要重新加密
- ADMIN角色不能修改自己的角色（避免误操作）

**界面要求**
- 用户编辑表单，预填充当前信息
- 密码输入框为空（不显示原密码）

**粒度评估**: S (Small) - 0.5天工作量，标准CRUD功能

---

### US007: 禁用/启用用户

**用户故事**
> 作为 管理员，
> 我希望 能够禁用或启用用户账号，
> 以便 控制用户的访问权限。

**功能要点**
- 用户列表中显示"启用"/"禁用"按钮
- 点击后切换用户状态（enabled字段）
- 禁用的用户无法登录

**业务规则**
- 禁用用户时，如果用户已登录，需要立即清除Session（可选）
- ADMIN角色不能禁用自己（避免误操作）

**界面要求**
- 用户列表中显示用户状态（启用/禁用）
- 状态切换按钮明显

**粒度评估**: S (Small) - 0.5天工作量，标准状态切换功能

---

### US008: 为用户分配角色

**用户故事**
> 作为 管理员，
> 我希望 能够为用户分配角色（ADMIN/DESIGNER/VIEWER），
> 以便 控制用户的功能权限。

**功能要点**
- 用户列表或编辑页面显示角色下拉框
- 选择角色并保存
- 角色变更立即生效（下次请求生效）

**业务规则**
- 系统只有三个固定角色：ADMIN、DESIGNER、VIEWER
- 一个用户只能有一个角色（单角色模式）
- 角色变更后，用户下次登录时生效

**界面要求**
- 角色下拉框显示三个选项
- 当前角色高亮显示

**粒度评估**: S (Small) - 0.5天工作量，角色分配功能

---

## 📝 功能分组3：设计端（DESIGNER）- 核心功能

### US010: SQL编辑器实现（CodeMirror）

**用户故事**
> 作为 设计者，
> 我希望 有一个基于CodeMirror的SQL编辑器，支持基础语法高亮和多行编辑，
> 以便 方便地编写和调试SQL查询语句，提升开发效率。

**功能要点**
- 集成CodeMirror 5.65.x（使用vue-codemirror封装）
- SQL语法高亮（text/x-mysql模式）
- 支持多行编辑
- 代码自动缩进
- 支持快捷键（Ctrl+S保存、Ctrl+Enter执行）
- 括号自动匹配和高亮
- 高亮当前行

**业务规则**
- 仅支持SELECT语句（其他语句不允许保存）
- SQL长度限制：不超过10000个字符
- 自动保存草稿（避免意外关闭丢失）

**界面要求**
- 全屏编辑模式（可选）
- 行号显示
- Material深色主题

**技术约束**: 使用CodeMirror 5.65.x + vue-codemirror 4.0.6
**MVP范围**: 语法高亮、多行编辑、Ctrl+S保存、Ctrl+Enter执行，不包含自动补全
**粒度评估**: M (Medium) - 2天工作量（预留0.5天处理集成问题）

---

### US011: 报表基本信息配置

**用户故事**
> 作为 设计者，
> 我希望 能够配置报表的基本信息（名称、描述），
> 以便 创建报表并说明其用途。

**功能要点**
- 填写报表名称（唯一，不能重复）
- 填写报表描述（多行文本）
- 自动记录创建人和创建时间

**业务规则**
- 报表名称唯一（不能与已有报表重复）
- 报表名称不能为空
- 报表描述可选

**界面要求**
- 简单的表单，包含名称和描述输入框
- 名称输入框限制50字符
- 描述输入框支持多行（Textarea）

**粒度评估**: S (Small) - 0.5天工作量，简单表单

---

### US012: 参数配置功能

**用户故事**
> 作为 设计者，
> 我希望 能够配置SQL查询参数（参数名、类型、默认值、是否必填），
> 以便 用户在查询时输入动态参数。

**功能要点**
- 添加参数（参数名、参数类型、默认值、是否必填、参数说明）
- 参数类型：STRING、NUMBER、DATE、DATETIME
- 参数占位符：SQL中使用`:paramName`表示参数
- 支持多个参数（最多20个）
- 参数排序（上移/下移）
- 删除参数

**业务规则**
- 参数名唯一（不能重复）
- 参数名只能包含字母、数字、下划线
- SQL中的参数占位符必须与配置的参数名匹配
- 必填参数不能有默认值（或默认值为空）

**界面要求**
- 参数配置表格，每行一个参数
- 支持快速添加和删除
- 参数排序按钮（上移/下移）

**粒度评估**: M (Medium) - 1.5天工作量，涉及参数提取和校验

---

### US013: 列配置功能（优化版）

**用户故事**
> 作为 设计者，
> 我希望 能够配置查询结果的列属性（显示名称、列宽、格式化），支持从SQL结果集自动提取字段名，
> 以便 用户看到友好的数据展示，减少手动配置工作量。

**前置依赖**: US010（SQL编辑器）、US017（报表预览，用于测试执行SQL）
**技术难点**: ResultSetMetaData列信息提取、类型映射（JDBC → 前端格式化类型）

**功能要点**
- 自动从SQL结果集提取字段名
- 配置每列的显示名称
- 配置列宽（像素）
- 配置数据格式化（日期格式、数字精度、货币符号）
- 列排序（上移/下移）
- 隐藏/显示列

**业务规则**
- 字段名来自SQL查询结果（自动提取）
- 显示名称默认等于字段名
- 列宽默认100像素
- 格式化类型：默认、日期（YYYY-MM-DD）、数字（保留2位小数）、货币（¥）

**界面要求**
- 列配置表格，每行一列
- 支持批量操作（全部显示/隐藏）

**粒度评估**: M (Medium) - 2天工作量（包含0.5天技术预研，动态列提取和配置）

---

### US014: 报表创建功能（优化版）

**用户故事**
> 作为 设计者，
> 我希望 能够创建新的报表，包括填写基本信息、编写SQL、配置参数和列，并通过测试执行验证SQL正确性，
> 以便 为用户提供数据查询功能。

**前置依赖**: US010（SQL编辑器）、US011（基本信息配置）、US012（参数配置）、US013（列配置）

**功能要点**
- 填写报表基本信息（名称、描述）
- 编写SQL查询语句
- 配置查询参数
- 配置列属性
- 保存报表（生成报表ID）

**业务规则**
- 创建报表时自动关联创建人
- 报表创建后默认状态为"草稿"
- 保存前必须通过测试执行（至少执行一次无错误）
- 保存成功后自动跳转到报表列表

**验收标准**:
- [ ] 报表名称不能与已有报表重复（保存时校验）
- [ ] SQL不能为空（前端校验）
- [ ] 保存前必须通过测试执行（至少执行一次无错误）
- [ ] SQL执行失败时显示具体错误信息（如语法错误、表不存在）
- [ ] 保存成功后跳转到报表列表，显示新建的报表

**界面要求**
- 多步骤表单（Step 1: 基本信息 → Step 2: SQL编辑 → Step 3: 参数配置 → Step 4: 列配置）
- 支持保存草稿

**粒度评估**: S (Small) - 1天工作量（整合功能为主，新增保存逻辑和校验）

---

### US015: 报表编辑功能

**用户故事**
> 作为 设计者，
> 我希望 能够编辑已有报表的配置，
> 以便 修正错误或优化报表功能。

**功能要点**
- 加载报表当前配置（基本信息、SQL、参数、列配置）
- 编辑所有可配置项
- 保存修改（更新updated_at字段）
- 记录修改日志（可选）

**业务规则**
- 只能编辑自己创建的报表（或管理员可编辑所有）
- 编辑后需要重新测试执行
- 如果报表已授权给用户，修改后需要通知用户（可选）

**界面要求**
- 与创建报表相同的表单，预填充当前配置
- 显示"最后修改时间"

**粒度评估**: S (Small) - 0.5天工作量，复用创建功能

---

### US016: 报表删除功能

**用户故事**
> 作为 设计者，
> 我希望 能够删除不再需要的报表，
> 以便 保持报表列表的整洁。

**功能要点**
- 从报表列表中选择报表并删除
- 删除前弹出确认对话框
- 删除报表同时删除关联的参数、列配置、权限记录
- 记录删除日志

**业务规则**
- 只能删除自己创建的报表（或管理员可删除所有）
- 删除后数据不可恢复（或软删除，可选）
- 删除前检查是否有用户正在使用该报表（可选）

**界面要求**
- 报表列表中显示"删除"按钮
- 确认对话框显示报表名称和警告信息

**粒度评估**: S (Small) - 0.5天工作量，标准删除功能

---

## 👁️ 功能分组4：设计端（DESIGNER）- 预览与授权

### US017: 报表预览（测试执行）（优化版）

**用户故事**
> 作为 设计者，
> 我希望 能够在保存报表前测试执行SQL查询，查看前100行结果并验证列配置的正确性，
> 以便 确保报表能够正常工作，避免保存错误配置。

**前置依赖**: 
- US010（SQL编辑器）
- US012（参数配置，生成参数输入表单）
- US013（列配置，提取字段并配置显示）
- US033（SQL安全校验，并行开发）

**技术要点**:
- 使用PreparedStatement参数化执行
- 查询超时控制：5秒
- 仅返回前100行数据（避免大数据量影响性能）
- 使用只读数据库账号

**功能要点**
- 点击"测试执行"按钮
- 弹出参数输入对话框（如果有参数）
- 执行SQL查询，返回前100行结果
- 显示查询结果表格（按列配置展示）
- 显示执行时间和结果行数
- 支持多次测试执行（修改参数后重新执行）

**业务规则**
- 测试执行时不保存报表
- 查询超时时间：5秒
- 最多返回100行数据（避免大数据量影响性能）
- 使用只读数据库账号执行查询

**界面要求**
- 参数输入表单（基于参数配置动态生成）
- 数据表格展示（支持横向滚动）
- 显示执行信息（执行时间、行数）

**粒度评估**: M (Medium) - 2.5天工作量（SQL执行引擎2天 + 前端动态渲染0.5天）

---

### US018: 为报表分配角色权限

**用户故事**
> 作为 设计者，
> 我希望 能够为报表分配角色权限（VIEWER/DESIGNER），
> 以便 控制哪些用户可以查看和使用该报表。

**功能要点**
- 显示三个固定角色：ADMIN、DESIGNER、VIEWER
- 勾选角色复选框，为报表分配权限
- ADMIN角色默认有所有权限（不可取消）
- 保存权限分配
- 显示已授权角色列表

**业务规则**
- 报表创建后默认仅创建人可访问
- ADMIN角色始终拥有所有报表的访问权限
- DESIGNER角色可以创建和编辑报表，并为报表授权
- VIEWER角色只能查看和执行被授权的报表

**界面要求**
- 权限分配表单，显示三个角色复选框
- 已授权角色高亮显示

**粒度评估**: M (Medium) - 2天工作量，涉及权限矩阵设计

---

## 🔍 功能分组5：用户端（VIEWER）- 查询功能

### US021: 查看授权报表列表

**用户故事**
> 作为 普通用户，
> 我希望 能够查看我有权访问的报表列表，
> 以便 选择报表进行查询。

**功能要点**
- 显示授权报表列表（卡片或表格）
- 显示报表名称、描述、创建人、创建时间
- 支持报表搜索（按名称模糊搜索）
- 点击报表进入查询页面

**业务规则**
- 仅显示已授权的报表（基于用户角色）
- ADMIN角色可查看所有报表
- 报表列表按创建时间倒序排列
- 禁用的用户无法查看报表列表

**界面要求**
- 报表列表，卡片或表格展示
- 搜索框位于列表顶部
- 显示报表数量

**粒度评估**: M (Medium) - 1.5天工作量，涉及权限过滤逻辑

---

### US022: 查看报表详情

**用户故事**
> 作为 普通用户，
> 我希望 能够查看报表的详细信息（名称、描述、参数说明），
> 以便 了解报表的用途和查询方式。

**功能要点**
- 显示报表名称和描述
- 显示报表创建人和创建时间
- 显示参数列表和参数说明（参数名、类型、是否必填、默认值）
- 显示"执行查询"按钮

**业务规则**
- 仅显示有权限的报表详情
- 访问无权限报表时返回403错误

**界面要求**
- 报表详情页，信息清晰展示
- 参数说明采用表格或列表

**粒度评估**: S (Small) - 0.5天工作量，简单展示页面

---

### US023: 参数输入表单（优化版）

**用户故事**
> 作为 普通用户，
> 我希望 系统根据报表参数配置自动生成输入表单，支持不同类型的输入控件（文本框、数字框、日期选择器），
> 以便 方便地输入查询条件，无需手动构造参数。

**前置依赖**: US012（参数配置，提供参数定义数据）
**支持的参数类型**: STRING（文本框）、NUMBER（数字框）、DATE（日期选择器）、DATETIME（日期时间选择器）

**功能要点**
- 根据报表参数配置动态生成表单
- 支持不同参数类型的输入控件（文本框、数字框、日期选择器）
- 显示参数默认值
- 必填参数校验
- 参数格式校验（如日期格式、数字范围）

**业务规则**
- 必填参数不能为空
- 日期参数格式：YYYY-MM-DD
- 数字参数仅允许输入数字
- 参数输入后自动保存到浏览器本地存储（下次自动填充）

**验收标准**:
- [ ] 不同参数类型对应正确的输入控件
- [ ] 必填参数校验（不能为空）
- [ ] 数据类型校验（数字/日期格式）
- [ ] 默认值自动填充
- [ ] 参数输入后保存到本地存储（下次自动填充）

**界面要求**
- 表单布局清晰，每个参数占一行
- 必填参数标识红色星号
- 参数说明显示在输入框下方

**粒度评估**: M (Medium) - 2天工作量（动态表单生成1.5天 + 本地存储0.5天）

---

### US024: 执行报表查询

**用户故事**
> 作为 普通用户，
> 我希望 能够执行报表查询，
> 以便 获取我需要的数据。

**功能要点**
- 点击"查询"按钮执行SQL
- 参数必填校验
- 显示加载动画（查询中）
- 查询成功后显示数据表格
- 查询失败时显示错误提示
- 记录查询日志（用户、报表、参数、时间）

**业务规则**
- 查询前校验用户权限
- 查询超时时间：5秒
- 查询结果最多返回5000行
- 超过5000行时提示用户添加筛选条件

**界面要求**
- "查询"按钮明显，点击后禁用（防止重复提交）
- 加载动画提示用户查询中
- 错误提示清晰友好

**粒度评估**: M (Medium) - 2天工作量，涉及SQL执行和权限校验

---

## 📊 功能分组6：用户端（VIEWER）- 展示与导出

### US025: 数据表格展示（优化版）

**用户故事**
> 作为 普通用户，
> 我希望 查询结果以表格形式清晰展示，支持动态列渲染和数据格式化（日期、数字、货币），
> 以便 方便地浏览和分析数据。

**前置依赖**: 
- US013（列配置，提供列元数据）
- US024（执行报表查询，提供数据源）
- US034（动态列渲染优化，并行开发或提前完成）

**技术方案**:
- Element UI Table组件
- v-for动态列渲染
- 数据格式化工具函数（formatDate、formatNumber、formatCurrency）

**功能要点**
- 使用Element UI Table组件展示数据
- 根据列配置显示列名（显示名称）
- 支持横向滚动（列多时）
- 支持列宽调整（可选）
- 显示总行数

**业务规则**
- 列名显示为配置的"显示名称"
- 日期字段格式化为YYYY-MM-DD
- 数字字段格式化（保留2位小数）
- 空值显示为"-"

**界面要求**
- 表格样式清晰，支持斑马纹
- 表头固定（滚动时不消失）
- 表格下方显示总行数

**粒度评估**: M (Medium) - 2天工作量（表格展示1天 + 格式化+样式优化1天）

---

### US026: 分页功能

**用户故事**
> 作为 普通用户，
> 我希望 查询结果支持分页展示，
> 以便 在数据量大时方便浏览。

**功能要点**
- 每页显示100条数据
- 显示分页控件（首页、上一页、下一页、尾页、跳转）
- 显示当前页码和总页数
- 分页在前端实现（一次性加载所有数据）

**业务规则**
- 默认显示第1页
- 每页固定100条
- 总数据量不超过5000行

**界面要求**
- 分页控件位于表格下方
- 显示"共X条数据，第Y页/共Z页"

**粒度评估**: S (Small) - 0.5天工作量，Element UI自带分页组件

---

### US028: Excel导出功能

**用户故事**
> 作为 普通用户，
> 我希望 能够将查询结果导出为Excel文件，
> 以便 在本地进行进一步分析或分享给他人。

**功能要点**
- 点击"导出Excel"按钮
- 生成Excel文件（.xlsx格式）
- 文件名格式：`报表名_YYYYMMDD_HHmmss.xlsx`
- 包含表头（显示名称）和数据行
- 浏览器自动下载文件

**业务规则**
- 限制导出行数：最多5000行
- 超过5000行时提示用户添加筛选条件
- 导出前校验用户权限
- 记录导出日志（用户、报表、导出时间、行数）

**技术要点**
- 后端：Apache POI生成Excel
- 前端：Axios下载文件流

**界面要求**
- "导出Excel"按钮位于表格上方
- 导出中显示进度提示
- 导出成功后自动下载

**粒度评估**: M (Medium) - 2天工作量，涉及POI Excel生成

---

## 🔐 功能分组7：权限控制层（RBAC）

### US029: 角色定义与权限矩阵

**用户故事**
> 作为 系统开发者，
> 我希望 定义三个固定角色及其权限矩阵，
> 以便 实现基于角色的权限控制。

**功能要点**
- 定义三个角色：ADMIN（管理员）、DESIGNER（设计者）、VIEWER（普通用户）
- 设计权限矩阵（角色 vs 功能模块）
- 权限映射：每个角色对应一组URL路径和操作权限
- 权限拦截器根据权限矩阵校验请求

**角色权限矩阵**:

| 功能模块 | ADMIN | DESIGNER | VIEWER |
|---------|-------|----------|--------|
| 用户管理（创建、编辑、禁用） | ✅ | ❌ | ❌ |
| 报表设计（创建、编辑、删除、授权） | ✅ | ✅ | ❌ |
| 报表查询（查看报表列表、执行查询、导出） | ✅ | ✅ | ✅ |

**技术要点**
- 权限矩阵存储（数据库或配置文件）
- 角色权限加载（系统启动时加载）
- 权限校验逻辑（拦截器中实现）

**粒度评估**: M (Medium) - 1.5天工作量，涉及权限设计

---

### US030: 权限拦截器实现

**用户故事**
> 作为 系统开发者，
> 我希望 实现权限拦截器，
> 以便 根据用户角色校验访问权限，拒绝未授权的请求。

**功能要点**
- 实现权限拦截器（Interceptor）
- 从Session中获取用户角色
- 根据URL路径和用户角色判断是否有权限
- 无权限时返回403错误（或跳转到无权限页面）

**业务规则**
- 所有API和页面都需要权限校验（除登录和静态资源）
- ADMIN角色拥有所有权限
- 权限校验失败时记录日志

**技术要点**
- SpringBoot拦截器（HandlerInterceptor）
- 权限矩阵匹配算法
- 403错误处理

**粒度评估**: M (Medium) - 1.5天工作量，涉及拦截器实现

---

### US032: 执行日志记录

**用户故事**
> 作为 系统开发者，
> 我希望 记录用户的报表执行日志（用户、报表、参数、执行时间、结果行数），
> 以便 追踪用户行为和排查问题。

**功能要点**
- 记录执行日志到数据库（execution_logs表）
- 日志字段：用户ID、报表ID、参数JSON、执行时间、结果行数、是否成功、错误信息
- 日志记录异步执行（不影响查询性能）
- 日志保留30天（自动清理）

**业务规则**
- 每次查询都记录日志（成功或失败）
- 日志记录不影响查询性能（异步记录）
- 日志数据用于审计和分析

**技术要点**
- 异步日志记录（线程池或消息队列）
- 定时任务清理过期日志

**粒度评估**: S (Small) - 1天工作量，日志记录功能

---

## ⚙️ 功能分组8：技术增强功能

### US033: SQL安全校验

**用户故事**
> 作为 系统开发者，
> 我希望 实现SQL安全校验机制，
> 以便 防止SQL注入和恶意查询。

**功能要点**
- SQL白名单校验：仅允许SELECT语句
- SQL黑名单校验：禁止DROP、DELETE、TRUNCATE、UPDATE、INSERT、ALTER、CREATE、EXEC等关键字
- PreparedStatement参数化执行
- 查询超时控制：5秒
- 使用只读数据库账号

**业务规则**
- 保存报表前必须通过SQL校验
- 测试执行和正式查询都使用相同的校验规则
- SQL包含危险关键字时拒绝保存并提示错误

**技术要点**
- 正则表达式匹配SQL关键字
- PreparedStatement防止SQL注入
- MySQL只读账号权限控制

**粒度评估**: M (Medium) - 1.5天工作量，涉及SQL解析和校验

---

### US034: 动态列渲染优化（提升为Must Have）

**用户故事**
> 作为 系统开发者，
> 我希望 实现一套统一的动态列渲染方案（后端列信息提取 + 前端动态组件），
> 以便 US013/US017/US025等Story可以复用此技术能力，避免重复开发，提升大数据量渲染性能。

**业务价值**:
- 统一列信息提取逻辑，减少重复代码
- 提升大数据量（>1000行）渲染性能（目标：1000行<3秒渲染）
- 提供可复用的Vue列渲染组件

**技术方案**:
- 后端：ResultSetMetaData提取列信息、JDBC类型 → 前端格式化类型映射
- 前端：Vue动态组件（DynamicTableColumn）、虚拟滚动（可选）

**依赖此Story的功能**: US013、US017、US025

**功能要点**
- 从ResultSet元数据提取列信息（列名、类型）
- 根据列类型自动选择格式化规则（日期、数字、字符串）
- Vue动态组件渲染（v-for + 动态列配置）
- 虚拟滚动（大数据量时，可选）

**业务规则**
- 列名优先使用配置的"显示名称"
- 未配置的列使用字段名
- 数据类型推断：DATE → 日期格式，DECIMAL → 数字格式

**技术要点**
- ResultSetMetaData提取列信息
- Vue动态组件渲染
- Element UI Table动态列

**粒度评估**: M (Medium) - 2天工作量（后端1天 + 前端1天）

---

## 📊 工作量统计与迭代规划

### 工作量汇总

**按粒度统计**:
- S (Small): 14个 × 0.5天 = 7人日
- M (Medium): 17个 × 1-2.5天 = 约32人日（含US000: 1天）
- **总计**: 约39人日

**按功能分组统计**:
- 基础设施层：5.5人日（5个Story，含US000: 1天）
- 管理端：2人日（4个Story）
- 设计端-核心：7天（7个Story）
- 设计端-预览与授权：4.5人日（2个Story）
- 用户端-查询：6人日（4个Story）
- 用户端-展示与导出：4.5人日（3个Story）
- 权限控制层：4人日（3个Story）
- 技术增强：3.5人日（2个Story）

---

### 迭代规划建议（2人团队）

#### Sprint 1（第1-2周，10个工作日）- 基础设施 + 管理端 + 设计端核心

**目标**: 搭建框架，完成报表设计功能

**Story列表**: US000-US016（17个Story）

**预估工作量**: 22人日

**关键里程碑**:
- Day 1: 开发环境搭建（US000）
- Day 2-3: 基础设施层（登录、Session、数据库初始化）
- Day 4-5: 管理端（用户管理、角色分配）
- Day 6-11: 设计端核心（SQL编辑器、参数/列配置、报表CRUD）

**交付物**:
- ✅ 开发环境搭建完成，前后端项目可正常启动
- ✅ 数据库表结构创建完成，初始化数据导入成功
- ✅ 用户能够登录系统
- ✅ 管理员能够管理用户和角色
- ✅ 设计者能够创建、编辑、删除报表
- ✅ SQL编辑器支持语法高亮和快捷键
- ✅ 参数和列配置功能完成

---

#### Sprint 2（第3周，5个工作日）- 设计端预览与授权 + 技术增强

**目标**: 完成报表预览和权限分配，实现SQL安全校验和动态列渲染

**Story列表**: US017-US018, US033-US034（4个Story）

**预估工作量**: 9人日

**关键里程碑**:
- Day 11-13: 报表预览（测试执行）
- Day 14: 权限分配功能
- Day 15-16: SQL安全校验
- Day 17-18: 动态列渲染优化

**交付物**:
- ✅ 设计者能够测试执行报表并查看结果
- ✅ 设计者能够为报表分配角色权限
- ✅ SQL安全校验防止恶意查询
- ✅ 动态列渲染优化性能

---

#### Sprint 3（第4周，5个工作日）- 用户端查询与展示 + 权限控制

**目标**: 完成用户端查询和展示功能，实现权限控制

**Story列表**: US021-US026, US028-US030, US032（10个Story）

**预估工作量**: 14.5人日

**关键里程碑**:
- Day 19-20: 用户端报表列表和详情
- Day 21-22: 参数输入表单和执行查询
- Day 23-24: 数据表格展示和分页
- Day 25-26: Excel导出
- Day 27-28: 权限控制层（角色权限矩阵、拦截器、执行日志）

**交付物**:
- ✅ 普通用户能够查看授权报表列表
- ✅ 普通用户能够输入参数并执行查询
- ✅ 查询结果以表格形式展示，支持分页
- ✅ 用户能够导出Excel文件
- ✅ 权限控制生效，用户只能访问授权的报表

---

## ✅ 验收标准检查清单

### 每个用户故事需满足INVEST原则

- [ ] **I - Independent (独立性)**: Story之间尽量独立，减少依赖
- [ ] **N - Negotiable (可协商性)**: 实现细节可与开发团队协商
- [ ] **V - Valuable (有价值性)**: 每个Story对用户有明确价值
- [ ] **E - Estimable (可估算性)**: 开发团队能估算工作量
- [ ] **S - Small (小型化)**: 单个Story在0.5-2.5天内可完成
- [ ] **T - Testable (可测试性)**: 有明确的验收标准

---

## 📝 备注说明

### 优化来源

本文档基于《程序员报表生成工具-INVEST评估报告.md》的优化建议生成，已应用以下优化：

1. **US010（SQL编辑器）**: 
   - ❌ 原方案："集成CodeMirror或Monaco Editor"（模糊）
   - ✅ 优化后："集成CodeMirror 5.65.x（使用vue-codemirror封装）"（明确）
   - 工作量调整：1.5天 → 2天

2. **US013（列配置）**:
   - ✅ 增加前置依赖说明（US010、US017）
   - ✅ 明确技术难点（ResultSetMetaData、类型映射）
   - 工作量调整：1.5天 → 2天

3. **US014（报表创建）**:
   - ✅ 增加详细验收标准（5项检查点）
   - ✅ 明确前置依赖（US010、US011、US012、US013）
   - 工作量调整：0.5天 → 1天

4. **US017（报表预览）**:
   - ✅ 明确技术依赖（US033 SQL安全校验）
   - ✅ 增加技术要点说明（PreparedStatement、超时控制）
   - 工作量调整：2天 → 2.5天

5. **US023（参数输入表单）**:
   - ✅ 明确支持的参数类型（STRING、NUMBER、DATE、DATETIME）
   - ✅ 增加验收标准（5项检查点）
   - 工作量调整：1.5天 → 2天

6. **US025（数据表格展示）**:
   - ✅ 明确依赖US034（动态列渲染优化）
   - ✅ 增加技术方案说明（Element UI Table、v-for、格式化函数）
   - 工作量调整：1.5天 → 2天

7. **US034（动态列渲染优化）**:
   - ✅ 优先级从Should提升为Must
   - ✅ 增加业务价值说明（统一列信息提取、性能提升、可复用组件）
   - ✅ 明确依赖此Story的功能（US013、US017、US025）
   - 工作量调整：1.5天 → 2天

---

### 文档状态

- ✅ **已完成**: Must Have故事提取
- ✅ **已应用**: INVEST评估优化建议
- ✅ **已确认**: 30个Must Have故事
- ✅ **已规划**: 3个Sprint（约4周）

---

**下一步行动**:
1. 产品经理评审Must Have故事清单
2. 开发团队进行Story Point评估
3. Sprint 1启动会，明确任务分工
4. 开始Sprint 1开发

---

**文档版本**: v1.0（基于INVEST评估优化）
**生成日期**: 2025-01-25
