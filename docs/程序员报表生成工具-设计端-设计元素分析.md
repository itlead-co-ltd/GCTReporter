# 程序员报表生成工具 - 设计端（DESIGNER）设计元素分析

> **文档类型**: UI/UX设计元素分析
> **生成日期**: 2025-12-26
> **分析范围**: 设计端（DESIGNER）9个用户故事
> **技术栈**: SpringBoot 2.7.x + Vue.js 2.6.x + Element UI + CodeMirror 5.65.x

---

## 📋 分析概述

本文档基于《程序员报表生成工具-Must-Have故事.md》中的设计端用户故事（US010-US018，共9个故事），提取并分析核心设计元素，为UI设计和前端开发提供规范化指引。

**涉及用户故事**:
- **核心功能**（7个）: US010（SQL编辑器）、US011（基本信息）、US012（参数配置）、US013（列配置）、US014（报表创建）、US015（报表编辑）、US016（报表删除）
- **预览与授权**（2个）: US017（报表预览）、US018（角色权限分配）

---

## 1️⃣ 页面结构分析

| 页面名称 | 主要功能 | 目标用户 | 关联用户故事 | 导航路径 |
|---------|---------|---------|-------------|---------|
| 报表列表页 | 查看、搜索、创建、编辑、删除报表 | DESIGNER | US014/US015/US016 | /reports/list |
| 报表创建页 | 多步骤表单创建新报表（基本信息→SQL编辑→参数配置→列配置） | DESIGNER | US010/US011/US012/US013/US014 | /reports/create |
| 报表编辑页 | 编辑已有报表的所有配置项 | DESIGNER | US010/US011/US012/US013/US015 | /reports/edit/:id |
| 报表预览页 | 测试执行SQL，查看前100行结果 | DESIGNER | US017 | /reports/preview/:id |
| 权限分配页 | 为报表分配角色权限（ADMIN/DESIGNER/VIEWER） | DESIGNER | US018 | /reports/permissions/:id |

**页面层级关系**:
```
报表列表页（入口）
├── 报表创建页（新建流程）
│   ├── Step 1: 基本信息配置（US011）
│   ├── Step 2: SQL编辑器（US010）
│   ├── Step 3: 参数配置（US012）
│   ├── Step 4: 列配置（US013）
│   └── 保存报表（US014）
├── 报表编辑页（编辑流程，复用创建页组件）（US015）
├── 报表预览页（测试执行）（US017）
├── 权限分配页（授权管理）（US018）
└── 删除报表（确认对话框）（US016）
```

---

## 2️⃣ UI组件清单

### 2.1 报表列表页组件

| 组件类型 | 组件名称 | 用途 | 状态说明 | 关联故事 | Element UI组件 |
|---------|---------|------|---------|---------|---------------|
| 搜索框 | reportSearchBox | 按报表名称模糊搜索 | focus边框高亮、loading搜索中 | US014/US015 | el-input |
| 按钮 | createReportBtn | 跳转到报表创建页 | default/hover/disabled | US014 | el-button type="primary" |
| 表格 | reportListTable | 显示报表列表（名称、描述、创建人、创建时间） | 行hover高亮、支持排序 | US014/US015/US016 | el-table |
| 操作列 | reportActionColumn | 编辑、预览、权限、删除按钮 | hover显示操作按钮 | US015/US016/US017/US018 | el-table-column + el-button |
| 确认对话框 | deleteConfirmDialog | 删除报表前确认 | 显示报表名称、警告信息 | US016 | el-dialog + el-button |

---

### 2.2 报表创建/编辑页组件（多步骤表单）

#### Step 1: 基本信息配置（US011）

| 组件类型 | 组件名称 | 用途 | 状态说明 | 关联故事 | Element UI组件 |
|---------|---------|------|---------|---------|---------------|
| 输入框 | reportNameInput | 输入报表名称（唯一） | default/focus/error（重名） | US011 | el-input maxlength="50" |
| 文本域 | reportDescTextarea | 输入报表描述（多行） | default/focus、字数统计 | US011 | el-input type="textarea" rows="3" |
| 步骤条 | createStepsNav | 显示创建进度（4步） | 当前步高亮、已完成步打勾 | US014 | el-steps |
| 按钮 | nextStepBtn | 进入下一步 | default/hover/disabled（验证失败） | US014 | el-button type="primary" |

---

#### Step 2: SQL编辑器（US010）

| 组件类型 | 组件名称 | 用途 | 状态说明 | 关联故事 | Element UI组件 |
|---------|---------|------|---------|---------|---------------|
| 代码编辑器 | sqlCodeMirrorEditor | 编写SQL查询语句 | 语法高亮、多行编辑、Ctrl+S保存、Ctrl+Enter执行 | US010 | vue-codemirror |
| 配置项 | - CodeMirror模式 | text/x-mysql | SQL语法高亮 | US010 | mode: 'text/x-mysql' |
| 配置项 | - CodeMirror主题 | material | Material深色主题 | US010 | theme: 'material' |
| 配置项 | - 行号显示 | 显示行号 | 辅助调试 | US010 | lineNumbers: true |
| 配置项 | - 括号匹配 | 自动匹配和高亮 | 提升编辑体验 | US010 | matchBrackets: true |
| 配置项 | - 当前行高亮 | 高亮编辑行 | 视觉焦点 | US010 | styleActiveLine: true |
| 配置项 | - 自动缩进 | 代码自动缩进 | 提升可读性 | US010 | autoIndent: true |
| 按钮 | testExecuteBtn | 测试执行SQL | default/hover/loading（执行中）/disabled | US017 | el-button type="success" |
| 按钮 | fullScreenBtn | 全屏编辑模式（可选） | default/hover/active（全屏） | US010 | el-button icon="el-icon-full-screen" |
| 提示信息 | sqlErrorAlert | SQL错误提示 | 显示错误类型和行号 | US010/US017 | el-alert type="error" |
| 提示信息 | sqlLengthCounter | SQL字符计数（限制10000字符） | 接近限制时warning | US010 | el-tag + 字符计数逻辑 |

---

#### Step 3: 参数配置（US012）

| 组件类型 | 组件名称 | 用途 | 状态说明 | 关联故事 | Element UI组件 |
|---------|---------|------|---------|---------|---------------|
| 按钮 | addParamBtn | 添加参数行 | default/hover/disabled（已达20个上限） | US012 | el-button type="text" icon="el-icon-plus" |
| 表格 | paramConfigTable | 参数配置表格（可编辑） | 行内编辑模式 | US012 | el-table |
| 输入框 | paramNameInput | 参数名称（唯一） | 校验规则：字母/数字/下划线 | US012 | el-input |
| 下拉框 | paramTypeSelect | 参数类型（STRING/NUMBER/DATE/DATETIME） | 4个固定选项 | US012 | el-select |
| 输入框 | paramDefaultInput | 参数默认值 | 根据类型动态切换输入控件 | US012 | el-input/el-date-picker |
| 复选框 | paramRequiredCheckbox | 是否必填 | checked/unchecked | US012 | el-checkbox |
| 输入框 | paramDescInput | 参数说明 | 简短描述 | US012 | el-input |
| 按钮 | paramMoveUpBtn | 参数上移 | 第一行disabled | US012 | el-button type="text" icon="el-icon-top" |
| 按钮 | paramMoveDownBtn | 参数下移 | 最后一行disabled | US012 | el-button type="text" icon="el-icon-bottom" |
| 按钮 | paramDeleteBtn | 删除参数 | 确认提示 | US012 | el-button type="text" icon="el-icon-delete" |

---

#### Step 4: 列配置（US013）

| 组件类型 | 组件名称 | 用途 | 状态说明 | 关联故事 | Element UI组件 |
|---------|---------|------|---------|---------|---------------|
| 按钮 | autoExtractColumnsBtn | 从SQL结果集自动提取列信息 | loading执行SQL | US013 | el-button type="primary" |
| 表格 | columnConfigTable | 列配置表格（可编辑） | 行内编辑模式 | US013 | el-table |
| 文本 | columnFieldName | 字段名（自动提取，不可编辑） | 灰色显示 | US013 | el-table-column |
| 输入框 | columnDisplayNameInput | 显示名称 | 默认等于字段名 | US013 | el-input |
| 输入框 | columnWidthInput | 列宽（像素） | 默认100、校验数字类型 | US013 | el-input-number |
| 下拉框 | columnFormatSelect | 格式化类型（默认/日期/数字/货币） | 4个固定选项 | US013 | el-select |
| 开关 | columnVisibleSwitch | 显示/隐藏列 | on/off | US013 | el-switch |
| 按钮 | columnMoveUpBtn | 列上移 | 第一列disabled | US013 | el-button type="text" icon="el-icon-top" |
| 按钮 | columnMoveDownBtn | 列下移 | 最后一列disabled | US013 | el-button type="text" icon="el-icon-bottom" |
| 按钮 | batchShowAllBtn | 批量显示所有列 | default/hover | US013 | el-button type="text" |
| 按钮 | batchHideAllBtn | 批量隐藏所有列 | default/hover | US013 | el-button type="text" |

---

#### 通用组件（所有步骤共用）

| 组件类型 | 组件名称 | 用途 | 状态说明 | 关联故事 | Element UI组件 |
|---------|---------|------|---------|---------|---------------|
| 步骤条 | createStepsNav | 显示创建/编辑进度（4步） | 当前步高亮、已完成步打勾 | US014/US015 | el-steps |
| 按钮 | prevStepBtn | 返回上一步 | default/hover/disabled（第1步） | US014/US015 | el-button |
| 按钮 | nextStepBtn | 进入下一步 | default/hover/disabled（验证失败） | US014/US015 | el-button type="primary" |
| 按钮 | saveDraftBtn | 保存草稿 | default/hover/loading（保存中） | US014/US015 | el-button type="info" |
| 按钮 | saveReportBtn | 保存报表（第4步） | default/hover/loading/disabled（未测试执行） | US014/US015 | el-button type="success" |
| 按钮 | cancelBtn | 取消并返回列表 | 二次确认 | US014/US015 | el-button |

---

### 2.3 报表预览页组件（US017）

| 组件类型 | 组件名称 | 用途 | 状态说明 | 关联故事 | Element UI组件 |
|---------|---------|------|---------|---------|---------------|
| 对话框 | paramInputDialog | 参数输入表单（动态生成） | 根据参数配置动态渲染 | US017 | el-dialog |
| 动态表单 | dynamicParamForm | 根据参数配置生成输入控件 | STRING→输入框、NUMBER→数字框、DATE→日期选择器 | US017 | el-form + el-form-item |
| 按钮 | executeQueryBtn | 执行查询 | default/hover/loading（执行中）/disabled | US017 | el-button type="primary" |
| 表格 | previewResultTable | 显示前100行查询结果 | 按列配置展示、支持横向滚动 | US017 | el-table + 动态列 |
| 提示信息 | executeInfoAlert | 执行信息（执行时间、行数、限制说明） | 蓝色提示框 | US017 | el-alert type="info" |
| 提示信息 | executeErrorAlert | 执行错误提示（语法错误、表不存在等） | 红色错误框 | US017 | el-alert type="error" |
| 按钮 | closePreviewBtn | 关闭预览，返回编辑 | default/hover | US017 | el-button |

---

### 2.4 权限分配页组件（US018）

| 组件类型 | 组件名称 | 用途 | 状态说明 | 关联故事 | Element UI组件 |
|---------|---------|------|---------|---------|---------------|
| 卡片 | permissionConfigCard | 权限分配表单容器 | 显示报表名称 | US018 | el-card |
| 复选框组 | roleCheckboxGroup | 三个固定角色（ADMIN/DESIGNER/VIEWER） | ADMIN默认勾选且禁用 | US018 | el-checkbox-group |
| 复选框 | adminRoleCheckbox | ADMIN角色（默认勾选，不可取消） | checked、disabled | US018 | el-checkbox disabled |
| 复选框 | designerRoleCheckbox | DESIGNER角色 | checked/unchecked | US018 | el-checkbox |
| 复选框 | viewerRoleCheckbox | VIEWER角色 | checked/unchecked | US018 | el-checkbox |
| 标签 | authorizedRoleTag | 已授权角色列表 | 高亮显示 | US018 | el-tag type="success" |
| 按钮 | savePermissionBtn | 保存权限分配 | default/hover/loading（保存中） | US018 | el-button type="primary" |
| 按钮 | cancelPermissionBtn | 取消并返回 | default/hover | US018 | el-button |

---

## 3️⃣ 数据字段定义

### 3.1 报表基本信息（US011）

| 字段名称 | 数据类型 | 是否必填 | 验证规则 | 默认值 | 说明 |
|---------|---------|---------|---------|--------|------|
| reportName | String | ✅ 是 | 非空、唯一、2-50字符、不含特殊字符 | - | 报表名称 |
| reportDesc | Text | ❌ 否 | 0-500字符 | - | 报表描述 |
| creatorId | Number | ✅ 是（自动） | 关联users.id | 当前登录用户 | 创建人ID |
| createdAt | DateTime | ✅ 是（自动） | YYYY-MM-DD HH:mm:ss | 当前时间 | 创建时间 |
| updatedAt | DateTime | ✅ 是（自动） | YYYY-MM-DD HH:mm:ss | 当前时间 | 最后修改时间 |
| status | Enum | ✅ 是 | draft/published | draft | 报表状态（草稿/已发布） |

---

### 3.2 SQL查询语句（US010）

| 字段名称 | 数据类型 | 是否必填 | 验证规则 | 默认值 | 说明 |
|---------|---------|---------|---------|--------|------|
| sqlContent | Text | ✅ 是 | 非空、1-10000字符、仅允许SELECT语句、参数占位符格式`:paramName` | - | SQL查询语句 |

---

### 3.3 参数配置（US012）

| 字段名称 | 数据类型 | 是否必填 | 验证规则 | 默认值 | 说明 |
|---------|---------|---------|---------|--------|------|
| reportId | Number | ✅ 是 | 关联reports.id | - | 所属报表ID |
| paramName | String | ✅ 是 | 非空、唯一（同一报表内）、仅字母/数字/下划线、1-50字符 | - | 参数名称 |
| paramType | Enum | ✅ 是 | STRING/NUMBER/DATE/DATETIME | STRING | 参数类型 |
| defaultValue | String | ❌ 否 | 根据paramType校验格式 | - | 参数默认值 |
| isRequired | Boolean | ✅ 是 | true/false | false | 是否必填 |
| paramDesc | String | ❌ 否 | 0-200字符 | - | 参数说明 |
| sortOrder | Number | ✅ 是 | 正整数 | 自增序列 | 参数排序序号 |

---

### 3.4 列配置（US013）

| 字段名称 | 数据类型 | 是否必填 | 验证规则 | 默认值 | 说明 |
|---------|---------|---------|---------|--------|------|
| reportId | Number | ✅ 是 | 关联reports.id | - | 所属报表ID |
| fieldName | String | ✅ 是 | 非空、来自SQL结果集 | - | 字段名（数据库列名） |
| displayName | String | ✅ 是 | 非空、1-50字符 | fieldName | 显示名称（前端表头） |
| columnWidth | Number | ✅ 是 | 50-500像素 | 100 | 列宽（像素） |
| formatType | Enum | ✅ 是 | DEFAULT/DATE/NUMBER/CURRENCY | DEFAULT | 格式化类型 |
| isVisible | Boolean | ✅ 是 | true/false | true | 是否显示 |
| sortOrder | Number | ✅ 是 | 正整数 | 自增序列 | 列排序序号 |

---

### 3.5 角色权限分配（US018）

| 字段名称 | 数据类型 | 是否必填 | 验证规则 | 默认值 | 说明 |
|---------|---------|---------|---------|--------|------|
| reportId | Number | ✅ 是 | 关联reports.id | - | 所属报表ID |
| roleCode | Enum | ✅ 是 | ADMIN/DESIGNER/VIEWER | - | 角色代码 |
| grantedAt | DateTime | ✅ 是（自动） | YYYY-MM-DD HH:mm:ss | 当前时间 | 授权时间 |

---

### 3.6 执行日志（US017相关）

| 字段名称 | 数据类型 | 是否必填 | 验证规则 | 默认值 | 说明 |
|---------|---------|---------|---------|--------|------|
| userId | Number | ✅ 是 | 关联users.id | - | 执行人ID |
| reportId | Number | ✅ 是 | 关联reports.id | - | 执行的报表ID |
| paramJson | JSON | ❌ 否 | JSON格式 | {} | 参数JSON |
| executeTime | DateTime | ✅ 是（自动） | YYYY-MM-DD HH:mm:ss | 当前时间 | 执行时间 |
| resultRows | Number | ❌ 否 | 0-5000 | 0 | 结果行数 |
| isSuccess | Boolean | ✅ 是 | true/false | false | 是否成功 |
| errorMsg | Text | ❌ 否 | 0-1000字符 | - | 错误信息 |
| executeDuration | Number | ❌ 否 | 毫秒 | - | 执行耗时（毫秒） |

---

## 4️⃣ 设计约束与建议

### 4.1 技术约束

| 约束类型 | 约束内容 | 建议处理方式 |
|---------|---------|-------------|
| 前端框架 | Vue.js 2.6.x（非3.x） | 使用Vue 2.x语法，避免Composition API |
| UI组件库 | Element UI 2.x | 统一使用el-前缀组件，避免混用其他UI库 |
| 代码编辑器 | CodeMirror 5.65.x（不使用Monaco Editor） | 使用vue-codemirror 4.0.6封装组件 |
| 浏览器兼容性 | Chrome 90+、Edge 90+、Firefox 88+ | 避免使用过新的ES特性，使用Babel转译 |
| 屏幕分辨率 | 最低1366×768 | 表格和编辑器支持横向滚动 |
| SQL长度限制 | 不超过10000字符 | CodeMirror需显示字符计数 |
| 参数数量限制 | 最多20个参数 | 添加参数时校验数量上限 |

---

### 4.2 交互约束

| 约束类型 | 约束内容 | 建议处理方式 |
|---------|---------|-------------|
| 步骤切换 | 创建报表采用多步骤表单（4步） | 使用el-steps组件，按步骤进度显示 |
| 数据校验 | 保存前必须通过测试执行（US014） | 保存按钮disabled，直到测试执行成功 |
| 参数依赖 | SQL占位符必须与参数配置匹配（US012） | 保存时校验，提示未配置的参数 |
| 列提取 | 列配置依赖SQL执行结果（US013） | 点击"自动提取"按钮触发SQL测试执行 |
| 权限控制 | ADMIN角色默认拥有所有报表权限（US018） | ADMIN复选框默认勾选且disabled |
| 删除确认 | 删除报表需二次确认（US016） | 使用el-dialog显示确认对话框 |

---

### 4.3 性能约束

| 约束类型 | 约束内容 | 建议处理方式 |
|---------|---------|-------------|
| SQL执行超时 | 测试执行超时5秒（US017） | 前端显示超时提示，后端设置查询超时 |
| 预览数据量 | 测试执行仅返回前100行（US017） | 后端LIMIT 100，前端提示"仅显示前100行" |
| 表格渲染 | 列配置表格可能有大量列（>50列） | 使用虚拟滚动或分页加载（可选） |
| CodeMirror性能 | 长SQL（>5000字符）编辑卡顿 | 开启延迟渲染（debounce），优化高亮性能 |

---

### 4.4 样式建议

#### 配色方案（Element UI默认主题）

| 颜色用途 | 色值 | 使用场景 |
|---------|------|---------|
| 主色调 | #409EFF | 主要按钮、链接、选中状态 |
| 成功色 | #67C23A | 保存成功、测试执行成功 |
| 警告色 | #E6A23C | 字符接近上限、必填提示 |
| 错误色 | #F56C6C | SQL错误、验证失败 |
| 信息色 | #909399 | 提示信息、辅助文字 |

#### CodeMirror主题（Material深色）

```css
/* 推荐使用Material主题，与Element UI风格协调 */
@import "codemirror/theme/material.css";
```

#### 布局间距规范

| 元素 | 间距规范 |
|------|---------|
| 表单字段垂直间距 | 20px（el-form-item默认） |
| 按钮组水平间距 | 10px |
| 卡片内边距 | 20px（el-card默认） |
| 表格行高 | 48px |
| 步骤条高度 | 80px |

---

### 4.5 待确认项（推断，待产品经理确认）

| 待确认项 | 推断方案 | 确认状态 |
|---------|---------|---------|
| SQL编辑器是否需要自动补全 | MVP阶段不实现，后续根据用户反馈考虑 | 🟡 待确认 |
| 报表草稿是否支持自动保存 | 使用localStorage自动保存，每30秒触发一次 | 🟡 待确认 |
| 列配置是否支持拖拽排序 | MVP阶段使用上移/下移按钮，后续考虑拖拽 | 🟡 待确认 |
| 权限分配是否支持批量操作 | MVP阶段单个报表逐个分配，后续考虑批量 | 🟡 待确认 |
| 预览结果是否支持导出 | 测试执行不支持导出，仅正式查询支持（US028） | ✅ 已确认 |
| 参数默认值是否支持表达式 | MVP阶段仅支持静态值，不支持动态表达式 | 🟡 待确认 |

---

## 5️⃣ 页面布局建议

### 5.1 报表创建页布局（多步骤表单）

```
┌─────────────────────────────────────────────────────────┐
│ 顶部导航栏（Logo + 用户信息 + 退出登录）                      │
├─────────────────────────────────────────────────────────┤
│ 面包屑导航：报表列表 > 创建报表                              │
├─────────────────────────────────────────────────────────┤
│ 步骤条（el-steps）                                        │
│ ● Step 1: 基本信息 → ○ Step 2: SQL编辑 → ○ Step 3: 参数 │
├─────────────────────────────────────────────────────────┤
│                                                         │
│ [Step 1: 基本信息配置]                                    │
│                                                         │
│ 报表名称*：[___________________________] (最多50字符)    │
│                                                         │
│ 报表描述：                                               │
│ ┌─────────────────────────────────────────┐            │
│ │ [多行文本输入框]                          │            │
│ │                                         │            │
│ └─────────────────────────────────────────┘            │
│                                                         │
├─────────────────────────────────────────────────────────┤
│ [取消] [保存草稿] [下一步 →]                              │
└─────────────────────────────────────────────────────────┘
```

---

### 5.2 SQL编辑器页面布局（Step 2）

```
┌─────────────────────────────────────────────────────────┐
│ 步骤条（el-steps）                                        │
│ ✓ Step 1: 基本信息 → ● Step 2: SQL编辑 → ○ Step 3: 参数 │
├─────────────────────────────────────────────────────────┤
│ SQL查询语句*  [测试执行] [全屏] 字符数: 256/10000        │
│ ┌─────────────────────────────────────────────────┐    │
│ │ 1  SELECT                                       │    │
│ │ 2      user_id,                                 │    │
│ │ 3      username,                                │    │
│ │ 4      created_at                               │    │
│ │ 5  FROM users                                   │    │
│ │ 6  WHERE created_at >= :startDate               │    │
│ │ 7      AND created_at <= :endDate               │    │
│ │ 8  ORDER BY created_at DESC;                    │    │
│ │ 9                                                │    │
│ │    (CodeMirror编辑器，支持语法高亮、行号、括号匹配)  │    │
│ └─────────────────────────────────────────────────┘    │
│                                                         │
│ [i] 提示：使用 :paramName 表示参数，如 :startDate         │
│                                                         │
├─────────────────────────────────────────────────────────┤
│ [← 上一步] [保存草稿] [下一步 →]                          │
└─────────────────────────────────────────────────────────┘
```

---

### 5.3 参数配置页面布局（Step 3）

```
┌─────────────────────────────────────────────────────────┐
│ 步骤条（el-steps）                                        │
│ ✓ Step 1: 基本信息 → ✓ Step 2: SQL编辑 → ● Step 3: 参数 │
├─────────────────────────────────────────────────────────┤
│ 参数配置（最多20个）  [+ 添加参数]                        │
│                                                         │
│ ┌───────────────────────────────────────────────────┐  │
│ │ 参数名*│类型*│默认值│必填│说明│排序│操作             │  │
│ ├───────────────────────────────────────────────────┤  │
│ │ startDate│DATE│2025-01-01│☑│开始日期│↑↓│🗑│      │  │
│ │ endDate│DATE│2025-12-31│☑│结束日期│↑↓│🗑│         │  │
│ │ status│STRING│active│☐│用户状态│↑↓│🗑│            │  │
│ └───────────────────────────────────────────────────┘  │
│                                                         │
│ [i] 参数名必须与SQL中的占位符匹配（:startDate）            │
│                                                         │
├─────────────────────────────────────────────────────────┤
│ [← 上一步] [保存草稿] [下一步 →]                          │
└─────────────────────────────────────────────────────────┘
```

---

### 5.4 列配置页面布局（Step 4）

```
┌─────────────────────────────────────────────────────────┐
│ 步骤条（el-steps）                                        │
│ ✓ Step 1 → ✓ Step 2 → ✓ Step 3 → ● Step 4: 列配置      │
├─────────────────────────────────────────────────────────┤
│ 列配置  [🔄 自动提取列信息] [全部显示] [全部隐藏]          │
│                                                         │
│ ┌───────────────────────────────────────────────────┐  │
│ │ 字段名│显示名称*│列宽(px)│格式化│显示│排序│操作    │  │
│ ├───────────────────────────────────────────────────┤  │
│ │ user_id│用户ID│100│默认│☑│↑↓│                    │  │
│ │ username│用户名│150│默认│☑│↑↓│                   │  │
│ │ created_at│创建时间│180│日期│☑│↑↓│               │  │
│ └───────────────────────────────────────────────────┘  │
│                                                         │
│ [i] 点击"自动提取"将执行SQL并获取列信息                     │
│                                                         │
├─────────────────────────────────────────────────────────┤
│ [← 上一步] [保存草稿] [💾 保存报表]                       │
└─────────────────────────────────────────────────────────┘
```

---

## 6️⃣ 关键交互流程

### 6.1 报表创建完整流程（US010-US014）

```mermaid
graph LR
    A[报表列表页] -->|点击"创建报表"| B[Step 1: 基本信息]
    B -->|填写名称、描述| C[Step 2: SQL编辑]
    C -->|编写SQL| D[测试执行SQL]
    D -->|执行成功| E[Step 3: 参数配置]
    D -->|执行失败| C
    E -->|配置参数| F[Step 4: 列配置]
    F -->|点击"自动提取"| G[执行SQL获取列信息]
    G -->|提取成功| H[配置列显示名称]
    H -->|点击"保存报表"| I[保存成功]
    I -->|跳转| A
```

---

### 6.2 参数输入与测试执行流程（US017）

```mermaid
graph LR
    A[SQL编辑器] -->|点击"测试执行"| B{是否有参数?}
    B -->|有参数| C[弹出参数输入对话框]
    B -->|无参数| E[直接执行SQL]
    C -->|填写参数| D[点击"执行查询"]
    D --> E[执行SQL]
    E -->|执行成功| F[显示前100行结果]
    E -->|执行失败| G[显示错误信息]
    F -->|关闭预览| A
    G -->|修改SQL| A
```

---

### 6.3 权限分配流程（US018）

```mermaid
graph LR
    A[报表列表页] -->|点击"权限"| B[权限分配页]
    B -->|勾选角色复选框| C[ADMIN/DESIGNER/VIEWER]
    C -->|点击"保存"| D[保存权限分配]
    D -->|保存成功| E[返回报表列表]
    E -->|显示已授权角色| A
```

---

## 7️⃣ 前端技术实现要点

### 7.1 CodeMirror集成（US010）

```javascript
// vue-codemirror配置示例
import { codemirror } from 'vue-codemirror'
import 'codemirror/lib/codemirror.css'
import 'codemirror/theme/material.css'
import 'codemirror/mode/sql/sql.js'

export default {
  components: {
    codemirror
  },
  data() {
    return {
      sqlContent: '',
      cmOptions: {
        mode: 'text/x-mysql',
        theme: 'material',
        lineNumbers: true,
        matchBrackets: true,
        styleActiveLine: true,
        autoIndent: true,
        extraKeys: {
          'Ctrl-S': (cm) => this.saveSql(),
          'Ctrl-Enter': (cm) => this.executeTest()
        }
      }
    }
  }
}
```

---

### 7.2 动态参数表单生成（US012/US017）

```javascript
// 根据参数配置动态生成表单控件
<el-form :model="paramValues" ref="paramForm">
  <el-form-item 
    v-for="param in paramConfig" 
    :key="param.paramName"
    :label="param.paramDesc || param.paramName"
    :prop="param.paramName"
    :rules="[
      { required: param.isRequired, message: '必填参数', trigger: 'blur' }
    ]"
  >
    <!-- STRING类型 -->
    <el-input 
      v-if="param.paramType === 'STRING'"
      v-model="paramValues[param.paramName]"
      :placeholder="param.defaultValue"
    />
    
    <!-- NUMBER类型 -->
    <el-input-number 
      v-if="param.paramType === 'NUMBER'"
      v-model="paramValues[param.paramName]"
      :placeholder="param.defaultValue"
    />
    
    <!-- DATE类型 -->
    <el-date-picker 
      v-if="param.paramType === 'DATE'"
      v-model="paramValues[param.paramName]"
      type="date"
      format="yyyy-MM-dd"
      value-format="yyyy-MM-dd"
    />
    
    <!-- DATETIME类型 -->
    <el-date-picker 
      v-if="param.paramType === 'DATETIME'"
      v-model="paramValues[param.paramName]"
      type="datetime"
      format="yyyy-MM-dd HH:mm:ss"
      value-format="yyyy-MM-dd HH:mm:ss"
    />
  </el-form-item>
</el-form>
```

---

### 7.3 动态列渲染（US013/US017）

```javascript
// 根据列配置动态渲染表格列
<el-table :data="tableData">
  <el-table-column
    v-for="column in visibleColumns"
    :key="column.fieldName"
    :prop="column.fieldName"
    :label="column.displayName"
    :width="column.columnWidth"
  >
    <template slot-scope="scope">
      <!-- 默认格式 -->
      <span v-if="column.formatType === 'DEFAULT'">
        {{ scope.row[column.fieldName] || '-' }}
      </span>
      
      <!-- 日期格式 -->
      <span v-if="column.formatType === 'DATE'">
        {{ formatDate(scope.row[column.fieldName]) }}
      </span>
      
      <!-- 数字格式（保留2位小数） -->
      <span v-if="column.formatType === 'NUMBER'">
        {{ formatNumber(scope.row[column.fieldName], 2) }}
      </span>
      
      <!-- 货币格式 -->
      <span v-if="column.formatType === 'CURRENCY'">
        ¥{{ formatNumber(scope.row[column.fieldName], 2) }}
      </span>
    </template>
  </el-table-column>
</el-table>
```

---

### 7.4 多步骤表单状态管理（US014）

```javascript
// 使用Vuex管理多步骤表单状态
const store = new Vuex.Store({
  state: {
    currentStep: 1,
    reportForm: {
      // Step 1: 基本信息
      reportName: '',
      reportDesc: '',
      
      // Step 2: SQL编辑
      sqlContent: '',
      
      // Step 3: 参数配置
      params: [],
      
      // Step 4: 列配置
      columns: []
    },
    testExecuteResult: null, // 测试执行结果
    isTestExecuted: false // 是否已测试执行
  },
  mutations: {
    setCurrentStep(state, step) {
      state.currentStep = step
    },
    updateBasicInfo(state, { reportName, reportDesc }) {
      state.reportForm.reportName = reportName
      state.reportForm.reportDesc = reportDesc
    },
    updateSqlContent(state, sqlContent) {
      state.reportForm.sqlContent = sqlContent
    },
    setTestExecuteResult(state, result) {
      state.testExecuteResult = result
      state.isTestExecuted = true
    }
  },
  actions: {
    async testExecuteSql({ commit, state }, params) {
      const result = await api.testExecute({
        sql: state.reportForm.sqlContent,
        params
      })
      commit('setTestExecuteResult', result)
      return result
    },
    async saveReport({ state }) {
      return await api.saveReport(state.reportForm)
    }
  }
})
```

---

## 8️⃣ 后端API接口规范

### 8.1 报表CRUD接口（US014/US015/US016）

| 接口名称 | 请求方法 | 路径 | 请求体 | 响应体 | 关联故事 |
|---------|---------|------|-------|-------|---------|
| 创建报表 | POST | /api/reports | { reportName, reportDesc, sqlContent, params[], columns[] } | { code, message, data: { reportId } } | US014 |
| 编辑报表 | PUT | /api/reports/:id | { reportName, reportDesc, sqlContent, params[], columns[] } | { code, message, data: { reportId } } | US015 |
| 删除报表 | DELETE | /api/reports/:id | - | { code, message } | US016 |
| 查询报表详情 | GET | /api/reports/:id | - | { code, message, data: { report, params, columns } } | US015 |
| 报表列表 | GET | /api/reports | ?keyword=&page=&size= | { code, message, data: { list, total } } | US014/US015 |

---

### 8.2 SQL测试执行接口（US017）

| 接口名称 | 请求方法 | 路径 | 请求体 | 响应体 | 关联故事 |
|---------|---------|------|-------|-------|---------|
| 测试执行SQL | POST | /api/reports/test-execute | { sql, params: { paramName: value } } | { code, message, data: { columns[], rows[], executeTime, rowCount } } | US017 |

**响应示例**:
```json
{
  "code": 200,
  "message": "执行成功",
  "data": {
    "columns": [
      { "fieldName": "user_id", "jdbcType": "INTEGER" },
      { "fieldName": "username", "jdbcType": "VARCHAR" },
      { "fieldName": "created_at", "jdbcType": "TIMESTAMP" }
    ],
    "rows": [
      { "user_id": 1, "username": "admin", "created_at": "2025-01-01 10:00:00" },
      { "user_id": 2, "username": "designer", "created_at": "2025-01-02 11:00:00" }
    ],
    "executeTime": 523,
    "rowCount": 2
  }
}
```

---

### 8.3 权限分配接口（US018）

| 接口名称 | 请求方法 | 路径 | 请求体 | 响应体 | 关联故事 |
|---------|---------|------|-------|-------|---------|
| 保存权限分配 | POST | /api/reports/:id/permissions | { roles: ["ADMIN", "DESIGNER", "VIEWER"] } | { code, message } | US018 |
| 查询已授权角色 | GET | /api/reports/:id/permissions | - | { code, message, data: { roles: [] } } | US018 |

---

## 9️⃣ 验收标准检查清单

### 9.1 UI组件验收标准

- [ ] ✅ CodeMirror集成成功，支持SQL语法高亮（US010）
- [ ] ✅ Ctrl+S保存、Ctrl+Enter执行快捷键生效（US010）
- [ ] ✅ 多步骤表单步骤条正确显示进度（US014）
- [ ] ✅ 参数输入表单根据参数配置动态生成控件（US012/US017）
- [ ] ✅ 列配置表格支持上移/下移/删除操作（US013）
- [ ] ✅ 预览结果表格按列配置展示（显示名称、列宽、格式化）（US017）
- [ ] ✅ 权限分配页ADMIN角色默认勾选且disabled（US018）
- [ ] ✅ 删除报表弹出确认对话框（US016）

---

### 9.2 数据校验验收标准

- [ ] ✅ 报表名称唯一性校验（前端+后端）（US011）
- [ ] ✅ SQL长度限制10000字符（前端提示）（US010）
- [ ] ✅ 参数名唯一性校验（同一报表内）（US012）
- [ ] ✅ 参数名格式校验（仅字母/数字/下划线）（US012）
- [ ] ✅ SQL占位符与参数配置匹配校验（US012）
- [ ] ✅ 保存前必须通过测试执行（保存按钮disabled）（US014）
- [ ] ✅ 必填参数校验（测试执行时）（US017）

---

### 9.3 交互流程验收标准

- [ ] ✅ 创建报表4步流程完整（基本信息→SQL→参数→列配置）（US014）
- [ ] ✅ 点击"自动提取列信息"触发SQL测试执行（US013）
- [ ] ✅ 测试执行显示前100行结果（US017）
- [ ] ✅ 测试执行显示执行时间和行数（US017）
- [ ] ✅ 测试执行失败显示具体错误信息（US017）
- [ ] ✅ 保存成功后跳转到报表列表（US014）
- [ ] ✅ 删除确认后才执行删除操作（US016）
- [ ] ✅ 权限分配保存后返回报表列表（US018）

---

### 9.4 性能验收标准

- [ ] ✅ SQL长度5000字符内编辑流畅（无卡顿）（US010）
- [ ] ✅ 测试执行超时5秒自动中断（US017）
- [ ] ✅ 预览结果表格渲染100行<2秒（US017）
- [ ] ✅ 列配置表格支持横向滚动（>10列）（US013）

---

## 🔟 附录：相关文档

| 文档名称 | 文档路径 | 用途 |
|---------|---------|------|
| Must Have故事列表 | `程序员报表生成工具-Must-Have故事.md` | 用户故事详细描述 |
| INVEST评估报告 | `程序员报表生成工具-INVEST评估报告.md` | 故事质量评估 |
| 数据库设计文档 | 待创建 | 表结构定义 |
| API接口文档 | 待创建 | 后端接口规范 |
| 前端组件规范 | 待创建 | 可复用组件库 |

---

**文档版本**: v1.0
**生成日期**: 2025-12-26
**分析人员**: AI系统
**待后续完善**: 数据库ER图、页面原型图、API详细文档

---

## ✅ 完成检查清单

- [x] ✅ 页面结构分析（5个核心页面）
- [x] ✅ UI组件清单（40+个组件）
- [x] ✅ 数据字段定义（6个数据实体）
- [x] ✅ 设计约束与建议（技术/交互/性能/样式）
- [x] ✅ 页面布局建议（4个关键页面布局图）
- [x] ✅ 关键交互流程（3个核心流程图）
- [x] ✅ 前端技术实现要点（4个技术方案）
- [x] ✅ 后端API接口规范（3类接口定义）
- [x] ✅ 验收标准检查清单（4个维度）
- [x] ✅ 可追溯性（所有设计元素关联用户故事）

---

**下一步行动**:
1. 产品经理确认待确认项
2. UI设计师基于本文档输出高保真原型
3. 前端工程师根据组件清单搭建页面结构
4. 后端工程师根据数据字段定义设计数据库表结构
5. 开发团队评审技术方案的可行性
